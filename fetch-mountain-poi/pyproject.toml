[project]
name = "fetch-mountain-poi"
version = "0.1.0"
description = "Resolve mountain POIs from the digitized AV-guide routes to OpenStreetMap coordinates."
requires-python = ">=3.13"
dependencies = [
    "rapidfuzz>=3.14.5",
]

[dependency-groups]
dev = ["pytest>=8"]

[tool.uv]
package = false
