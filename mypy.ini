; Shared mypy config for the whole repo. mypy does not walk up to find a
; config, so invoke it with --config-file pointing here (see CLAUDE.md / CI).
; Kept at defaults; only the third-party stubs we don't ship are silenced.
[mypy]
python_version = 3.13
ignore_missing_imports = True
