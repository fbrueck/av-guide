// React UI (sidebar, detail panel, popup, …) speaking domain types only.
// Populated by later tickets; if a component needs the map it extends
// src/map/'s imperative API rather than touching maplibre-gl directly.
export {};
